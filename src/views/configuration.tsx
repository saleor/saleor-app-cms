import { ConfigurationForm } from "../components/ConfigurationForm";

export const Configuration = () => {
  return (
    <section>
      <h2>Configuration</h2>
      <ConfigurationForm />
    </section>
  );
};
